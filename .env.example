# Sandbox por padrão. Use "production" só depois de validar o fluxo completo.
ASAAS_ENVIRONMENT=sandbox

# Chave gerada em Asaas > Integrações > API Key.
# A API do Asaas usa header access_token no servidor.
ASAAS_ACCESS_TOKEN=
